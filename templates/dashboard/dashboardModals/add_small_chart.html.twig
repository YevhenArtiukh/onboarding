<div id="modal_add_small_chart" class="modal fade" tabindex="-1"   aria-hidden="true">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title" id="myModalLabel"> Dodaj wykres</h4>
                <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
            </div>
            {{ form_start(formSmallChart) }}
            <div class="modal-body">
                <div class="form-group">
                    {{ form_label(formSmallChart.widget) }}
                    {{ form_errors(formSmallChart.widget) }}
                    {{ form_widget(formSmallChart.widget) }}
                </div>
            </div>
            <div class="modal-footer">
                <button class="btn btn-info" type="submit" >Zapisz</button>
            </div>
            {{ form_end(formSmallChart) }}
        </div>
        <!-- /.modal-content -->
    </div>
    <!-- /.modal-dialog -->
</div>