{% extends 'base.html.twig' %}

{% block title %}Zmiana roli{% endblock %}

{% block body %}
    <div class="page-wrapper">
        <div class="row page-titles">
            <div class="col-12 align-self-center">
                <h3 class="text-themecolor mb-0">Zmiana roli</h3>
            </div>
        </div>
        <div class="container-fluid">
            <div class="row">
                <div class="col-12">
                    <div class="card">
                        <div class="card-body">
                            {{ form_start(form) }}
                            <div class="form-body">
                                <div class="row">
                                    <div class="col-12">
                                        {{ form_row(form.name) }}
                                    </div>
                                </div>
                                <div class="row">
                                    {% for permission in form.permissions %}
                                        <div class="col-md-12 mb-1">
                                            {{ form_widget(permission) }}
                                            {{ form_label(permission) }}
                                        </div>
                                    {% endfor %}
                                </div>
                            </div>
                            <div class="form-actions">
                                <div class="text-right">
                                    <button type="submit" class="btn btn-info">Zmień</button>
                                    <a class="btn btn-dark" href="{{ path('roles') }}">Anuluj</a>
                                </div>
                            </div>
                            {{ form_end(form) }}
                        </div>
                    </div>
                </div>
            </div>
        </div>
        {{ parent() }}
    </div>
{% endblock %}